{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices"],
  "forkProcessing": "enabled",
  "mode": "full",
  "enabledManagers": [
    "poetry",
    "github-actions",
    "git-submodules",
    "custom.regex"
  ],
  "timezone": "America/Chicago",
  "updateNotScheduled": true,
  "dependencyDashboard": true,
  "dependencyDashboardTitle": "Dependency Updates (mtjk)",
  "automerge": false,
  "automergeType": "pr",
  "platformAutomerge": false,
  "separateMajorMinor": true,
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "packageRules": [
    {
      "description": "Automerge all non-major dependency updates",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest",
        "lockFileMaintenance"
      ],
      "automerge": true,
      "minimumReleaseAge": "12 hours"
    },
    {
      "description": "Hold pandas on v2 until Python/compatibility migration is complete",
      "matchPackageNames": ["pandas"],
      "allowedVersions": "<3"
    },
    {
      "description": "Manually reviewed 2026-09-16 (see DEPENDENCY_POLICY.md): release-sparse but source-active dependencies; suppress the release-age abandonment signal",
      "matchManagers": ["poetry"],
      "matchPackageNames": ["pyserial", "segno"],
      "abandonmentThreshold": null
    },
    {
      "description": "Keep bleak updates manual for planned migration work",
      "matchPackageNames": ["bleak"],
      "automerge": false
    },
    {
      "description": "Regenerate protobuf outputs before manually merging submodule updates",
      "matchManagers": ["git-submodules"],
      "matchDepNames": ["protobufs"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest", "pinDigest"],
      "automerge": false,
      "minimumReleaseAgeBehaviour": "timestamp-optional"
    },
    {
      "description": "Automerge meshtasticd Docker image updates (digest + version)",
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["meshtastic/meshtasticd"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest", "pinDigest"],
      "automerge": true
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track meshtasticd Docker image in CI workflows and shell scripts",
      "managerFilePatterns": [
        "/^\\.github/workflows/[^/]+\\.ya?ml$/",
        "/^bin/run-.*-with-meshtasticd\\.sh$/"
      ],
      "matchStrings": [
        "meshtastic/meshtasticd:(?<currentValue>[^@\\s]+)@(?<currentDigest>sha256:[a-f0-9]+)"
      ],
      "depNameTemplate": "meshtastic/meshtasticd",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Keep the Poetry installer version aligned across CI and container builds",
      "managerFilePatterns": [
        "/^Containerfile$/",
        "/^\\.github/workflows/[^/]+\\.ya?ml$/"
      ],
      "matchStrings": ["poetry==(?<currentValue>[^\\s\"']+)"],
      "depNameTemplate": "poetry",
      "datasourceTemplate": "pypi",
      "versioningTemplate": "pep440"
    },
    {
      "customType": "regex",
      "description": "Keep the container's Poetry export plugin reproducibly pinned",
      "managerFilePatterns": ["/^Containerfile$/"],
      "matchStrings": ["poetry-plugin-export==(?<currentValue>[^\\s\"']+)"],
      "depNameTemplate": "poetry-plugin-export",
      "datasourceTemplate": "pypi",
      "versioningTemplate": "pep440"
    }
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "git-submodules": {
    "enabled": true
  }
}
